# for postgresql:
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql_psycopg2",
        "NAME": "mtrack",
        "USER": "postgres",
        "PASSWORD": "test",
        "HOST": "localhost",
    }
}

